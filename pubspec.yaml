name: app_tienda_mobile
description: Aplicacion para pequenos negocios MINTIC 2022

publish_to:
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.4
  sqflite: ^2.0.0+4
  google_maps_flutter: ^2.1.1
  flutter_email_sender: ^5.0.2
  firebase_core: ^1.10.5
  firebase_messaging: ^11.2.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  flutter_launcher_icons: "^0.8.0"

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/TuComercio.png"

flutter:

  uses-material-design: true
  assets:
    - images/

